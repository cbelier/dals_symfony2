{% extends 'Back/LayoutBackOffice.html.twig' %}

{% block insertionModulesBackOffice %}

    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Les Vidéos ATTENTION CE SERVICE EST EN COURS DE DEVELOPPEMENT  NE PAS L'UTILISER</h1>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">Les vidéos </div>
                    <!-- /.panel-heading -->
                    <div class="panel-body">
                        <div class="table-responsive">
                            <table class="table table-hover" >
                                <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Danse</th>
                                    <th>Titre</th>
                                    <th>Nom complet</th>
                                    <th>Danseurs</th>
                                    <th>Likes</th>
                                    <th>Saison</th>
                                    <th>Options</th>
                                </tr>
                                </thead>
                                <tbody class="tableBodyVideo">
                                {#{% for video in videos %}
                                    <tr class="inputVideo ligne{{ video.id }}" >
                                        <td>{{ video.id }}</td>
                                        <td data-search="{{ video.nameDanse }}"><input type="text"   data-id="{{ video.id }}" data-type="nameDanse" value="{{ video.nameDanse |default('-')}}" readonly="true"></td>
                                        <td data-search="{{ video.titleVideo }}"><input type="text"   data-id="{{ video.id }}" data-type="titleVideo" value="{{ video.titleVideo |default('-')}}" readonly="true"></td>
                                        <td data-search="{{ video.nameVideo }}"><input type="text"   data-id="{{ video.id }}" data-type="nameVideo" value="{{ video.nameVideo |default('-')}}" readonly="true"></td>
                                        <td data-search="{{ video.firstnameCelebrity|default('-') }}"><input type="text"   data-id="{{ video.id }}" data-type="celebrity" value="{{ video.firstnameCelebrity |default('-')}}" readonly="true"></td>
                                        <td data-search="{{ video.likeVideo }}"><input type="number"   data-id="{{ video.id }}" data-type="likeVideo" value="{{ video.likeVideo |default('-')}}" readonly="true"></td>
                                        <td data-search="{{ video.nameSaison }}"><input type="text"   data-id="{{ video.id }}" data-type="nameSaison" value="{{ video.nameSaison |default('-')}}" readonly="true"></td>
                                        <td class="supprimerVideo" data-id="{{ video.id }}"><i class="fa fa-trash-o" aria-hidden="true"></i></td>
                                    </tr>
                                {% endfor %}#}

                                <tr>
                                    {{ form_start(formUpdateVideo) }}
                                    <td></td>
                                    <td>
                                        {{ form_widget(formUpdateVideo.danseId) }}
                                        {{ form_errors(formUpdateVideo.danseId) }}
                                    </td>
                                    <td>
                                        {{ form_widget(formUpdateVideo.titleVideo) }}
                                        {{ form_errors(formUpdateVideo.titleVideo) }}
                                    </td>
                                    <td>
                                        {{ form_widget(formUpdateVideo.nameVideo) }}
                                        {{ form_errors(formUpdateVideo.nameVideo) }}
                                    </td>
                                    <td>
                                        {{ form_widget(formUpdateVideo.celebrity) }}
                                        {{ form_errors(formUpdateVideo.celebrity) }}
                                    </td>
                                    <td>
                                        {{ form_widget(formUpdateVideo.likeVideo) }}
                                        {{ form_errors(formUpdateVideo.likeVideo) }}
                                    </td>
                                    <td>
                                        {{ form_widget(formUpdateVideo.saison) }}
                                        {{ form_errors(formUpdateVideo.saison) }}
                                    </td>
                                    <td></td>
                                        {{ form_end(formUpdateVideo) }}   
                                </tr>



                                </tbody>
                                <tfoot>
                                <tr>
                                    {{ form_start(formCreateVideo) }}
                                    <td></td>
                                    <td>
                                        {{ form_widget(formCreateVideo.danseId) }}
                                        {{ form_errors(formCreateVideo.danseId) }}
                                    </td>
                                    <td>
                                        {{ form_widget(formCreateVideo.titleVideo) }}
                                        {{ form_errors(formCreateVideo.titleVideo) }}
                                    </td>
                                    <td>
                                        {{ form_widget(formCreateVideo.nameVideo) }}
                                        {{ form_errors(formCreateVideo.nameVideo) }}
                                    </td>
                                    <td>
                                        {{ form_widget(formCreateVideo.celebrity) }}
                                        {{ form_errors(formCreateVideo.celebrity) }}
                                    </td>
                                    <td>
                                        {{ form_widget(formCreateVideo.likeVideo) }}
                                        {{ form_errors(formCreateVideo.likeVideo) }}
                                    </td>
                                    <td>
                                        {{ form_widget(formCreateVideo.saison) }}
                                        {{ form_errors(formCreateVideo.saison) }}
                                    </td>
                                    <td><input type="submit" class="btn btn-info btn-block btn-small" /></td>
                                        {{ form_end(formCreateVideo) }}   
                                </tr>

                                </tfoot>
                            </table>
                        </div>
                    </div>
                    <!-- /.panel-body -->
                </div>
                <!-- /.panel -->
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /#page-wrapper -->

{% endblock %}

{% block js %}
    <script src="{{ asset('dist/js/backoffice/video.js') }}"></script>

{% endblock %}